{
  "plugins": [
    {
      "id": "dv-studio-master",
      "name": "DVGE Studio Master",
      "description": "El lienzo definitivo para desarrolladores. Renderiza HTML, CSS y JS puro con sincronización total de frames y soporte para IA.",
      "version": "5.8.0",
      "author": "Jonatan Barón",
      "updatedAt": "2026-04-30T09:00:00.000Z",
      "files": {
        "manifest": {
          "id": "dv-studio-master",
          "name": "DVGE Studio Master",
          "description": "Renderizador de código profesional.",
          "version": "5.8.0",
          "author": "Jonatan Barón",
          "presets": ["branding", "motion", "layout"],
          "schema": [
            { "id": "masterRules", "type": "prompt", "label": "Contexto para IA", "defaultValue": "Reglas" },
            { "id": "htmlCode", "type": "code", "label": "Estructura HTML", "defaultValue": "<div id=\"studio-root\" class=\"dv-safe-area\"><h1 id=\"studio-title\">DVGE STUDIO</h1><div id=\"studio-bar\"></div></div>" },
            { "id": "cssCode", "type": "code", "label": "Estilos CSS", "defaultValue": "#studio-root { display: flex; flex-direction: column; align-items: center; justify-content: center; height: 100%; } #studio-title { color: var(--accent); font-size: 80px; font-weight: 900; margin: 0; } #studio-bar { width: 200px; height: 4px; background: white; margin-top: 10px; }" },
            { "id": "jsCode", "type": "code", "label": "Lógica JS", "defaultValue": "window.renderDVGE = (frame, props, ctx) => { const title = ctx.root.getElementById('studio-title'); const bar = ctx.root.getElementById('studio-bar'); if (!title || !bar) return; const ip = ctx.timeline.introProgress; title.style.opacity = ip; title.style.transform = `translateY(${ctx.utils.lerp(20, 0, ip)}px)`; bar.style.width = `${ctx.utils.lerp(0, 400, ctx.utils.spring(ip))}px`; };" }
          ]
        },
        "html": "<div id=\"dv-master-host\"><style id=\"dv-master-styles\"></style><div id=\"dv-master-canvas\" style=\"width: 1920px; height: 1080px; position: relative; overflow: hidden;\"></div></div>",
        "css": "#dv-master-host { width: 100%; height: 100%; background: transparent; will-change: transform; contain: paint; } #dv-master-canvas { transform-origin: top left; }",
        "js": "dvEngine.register({ awake: (ctx) => { ctx.state.lastHtml = null; ctx.state.lastCss = null; ctx.state.lastJs = null; ctx.state.injectedFn = null; ctx.state.childCtx = null; ctx.refs.canvas = ctx.root.getElementById('dv-master-canvas'); ctx.refs.styleTag = ctx.root.getElementById('dv-master-styles'); }, update: (ctx) => { const { props, refs, state, frame } = ctx; if (!refs.canvas || !refs.styleTag) return; if (state.lastHtml !== props.htmlCode) { refs.canvas.innerHTML = props.htmlCode || ''; state.lastHtml = props.htmlCode; state.injectedFn = null; state.lastJs = null; } if (state.lastCss !== props.cssCode) { refs.styleTag.textContent = props.cssCode || ''; state.lastCss = props.cssCode; } const currentJs = (props.jsCode || '').trim(); if (currentJs && state.lastJs !== currentJs) { state.lastJs = currentJs; try { const sandbox = { renderDVGE: null }; const sandboxCode = `(function(sandbox, ctx) { \"use strict\"; const window = { renderDVGE: null }; ${currentJs} \\n sandbox.renderDVGE = window.renderDVGE; })(arguments[0], arguments[1])`; const fn = new Function(sandboxCode); fn(sandbox, ctx); if (sandbox.renderDVGE) { state.injectedFn = sandbox.renderDVGE; state.childCtx = { ...ctx, state: {}, refs: {}, parent: ctx }; } } catch (e) { console.error('[Studio Master] JS Error:', e.message); } } if (state.injectedFn) { try { state.childCtx.frame = frame; state.childCtx.props = props; state.childCtx.timeline = ctx.timeline; state.injectedFn(frame, props, state.childCtx); } catch (e) {} } } });"
      }
    },
    {
      "id": "template-lower-third",
      "name": "Lower Third Base",
      "description": "Plantilla base de Lower Third, construida sobre el motor Studio Master. Editable en código.",
      "version": "1.0.0",
      "author": "Jonatan Barón ",
      "updatedAt": "2026-04-30T09:00:00.000Z",
      "files": {
        "manifest": {
          "id": "template-lower-third",
          "name": "Lower Third Base",
          "description": "Plantilla base de Lower Third.",
          "version": "1.0.0",
          "author": "Jonatan Barón",
          "presets": ["branding", "motion"],
          "schema": [
            { "id": "masterRules", "type": "prompt", "label": "Contexto para IA", "defaultValue": "Reglas" },
            { "id": "htmlCode", "type": "code", "label": "Estructura HTML", "defaultValue": "<div id=\"lt-root\">\n  <div id=\"lt-name\">Nombre Apellido</div>\n  <div id=\"lt-role\">Cargo Profesional</div>\n</div>" },
            { "id": "cssCode", "type": "code", "label": "Estilos CSS", "defaultValue": "#lt-root { position: absolute; bottom: 100px; left: 100px; display: flex; flex-direction: column; background: rgba(0,0,0,0.8); border-left: 6px solid var(--accent); padding: 20px 40px; border-radius: 4px; overflow: hidden; }\n#lt-name { color: white; font-size: 60px; font-weight: bold; margin: 0; line-height: 1; }\n#lt-role { color: #aaa; font-size: 30px; margin-top: 10px; line-height: 1; }" },
            { "id": "jsCode", "type": "code", "label": "Lógica JS", "defaultValue": "window.renderDVGE = (frame, props, ctx) => {\n  const root = ctx.root.getElementById('lt-root');\n  if (!root) return;\n  const ip = ctx.timeline.introProgress;\n  root.style.opacity = ip;\n  root.style.transform = `translateX(${ctx.utils.lerp(-50, 0, ctx.utils.easeOutCubic(ip))}px)`;\n};" }
          ]
        },
        "html": "<div id=\"dv-master-host\"><style id=\"dv-master-styles\"></style><div id=\"dv-master-canvas\" style=\"width: 1920px; height: 1080px; position: relative; overflow: hidden;\"></div></div>",
        "css": "#dv-master-host { width: 100%; height: 100%; background: transparent; will-change: transform; contain: paint; } #dv-master-canvas { transform-origin: top left; }",
        "js": "dvEngine.register({ awake: (ctx) => { ctx.state.lastHtml = null; ctx.state.lastCss = null; ctx.state.lastJs = null; ctx.state.injectedFn = null; ctx.state.childCtx = null; ctx.refs.canvas = ctx.root.getElementById('dv-master-canvas'); ctx.refs.styleTag = ctx.root.getElementById('dv-master-styles'); }, update: (ctx) => { const { props, refs, state, frame } = ctx; if (!refs.canvas || !refs.styleTag) return; if (state.lastHtml !== props.htmlCode) { refs.canvas.innerHTML = props.htmlCode || ''; state.lastHtml = props.htmlCode; state.injectedFn = null; state.lastJs = null; } if (state.lastCss !== props.cssCode) { refs.styleTag.textContent = props.cssCode || ''; state.lastCss = props.cssCode; } const currentJs = (props.jsCode || '').trim(); if (currentJs && state.lastJs !== currentJs) { state.lastJs = currentJs; try { const sandbox = { renderDVGE: null }; const sandboxCode = `(function(sandbox, ctx) { \"use strict\"; const window = { renderDVGE: null }; ${currentJs} \\n sandbox.renderDVGE = window.renderDVGE; })(arguments[0], arguments[1])`; const fn = new Function(sandboxCode); fn(sandbox, ctx); if (sandbox.renderDVGE) { state.injectedFn = sandbox.renderDVGE; state.childCtx = { ...ctx, state: {}, refs: {}, parent: ctx }; } } catch (e) { console.error('[Studio Master] JS Error:', e.message); } } if (state.injectedFn) { try { state.childCtx.frame = frame; state.childCtx.props = props; state.childCtx.timeline = ctx.timeline; state.injectedFn(frame, props, state.childCtx); } catch (e) {} } } });"
      }
    },
    {
      "id": "template-ticker",
      "name": "News Ticker Base",
      "description": "Plantilla base de Ticker de noticias deslizable, basada en Studio Master.",
      "version": "1.0.0",
      "author": "Jonatan Barón",
      "updatedAt": "2026-04-30T09:00:00.000Z",
      "files": {
        "manifest": {
          "id": "template-ticker",
          "name": "News Ticker Base",
          "description": "Plantilla base de Ticker.",
          "version": "1.0.0",
          "author": "Jonatan Barón",
          "presets": ["branding", "motion"],
          "schema": [
            { "id": "masterRules", "type": "prompt", "label": "Contexto para IA", "defaultValue": "Reglas" },
            { "id": "htmlCode", "type": "code", "label": "Estructura HTML", "defaultValue": "<div id=\"ticker-root\">\n  <div id=\"ticker-label\">BREAKING</div>\n  <div id=\"ticker-track\">\n    <div id=\"ticker-content\">NOTICIA IMPORTANTE DE ÚLTIMA HORA EN DIRECTO</div>\n  </div>\n</div>" },
            { "id": "cssCode", "type": "code", "label": "Estilos CSS", "defaultValue": "#ticker-root { position: absolute; bottom: 50px; left: 50px; right: 50px; height: 60px; display: flex; background: rgba(0,0,0,0.8); border-radius: 8px; overflow: hidden; }\n#ticker-label { background: var(--accent); color: white; padding: 0 30px; display: flex; align-items: center; font-size: 30px; font-weight: bold; z-index: 2; }\n#ticker-track { flex: 1; position: relative; overflow: hidden; display: flex; align-items: center; }\n#ticker-content { position: absolute; white-space: nowrap; color: white; font-size: 30px; left: 100%; }" },
            { "id": "jsCode", "type": "code", "label": "Lógica JS", "defaultValue": "window.renderDVGE = (frame, props, ctx) => {\n  const content = ctx.root.getElementById('ticker-content');\n  const root = ctx.root.getElementById('ticker-root');\n  if (!content || !root) return;\n  const ip = ctx.timeline.introProgress;\n  root.style.opacity = ip;\n  root.style.transform = `translateY(${ctx.utils.lerp(20, 0, ip)}px)`;\n  const speed = 5;\n  content.style.transform = `translateX(-${frame * speed}px)`;\n};" }
          ]
        },
        "html": "<div id=\"dv-master-host\"><style id=\"dv-master-styles\"></style><div id=\"dv-master-canvas\" style=\"width: 1920px; height: 1080px; position: relative; overflow: hidden;\"></div></div>",
        "css": "#dv-master-host { width: 100%; height: 100%; background: transparent; will-change: transform; contain: paint; } #dv-master-canvas { transform-origin: top left; }",
        "js": "dvEngine.register({ awake: (ctx) => { ctx.state.lastHtml = null; ctx.state.lastCss = null; ctx.state.lastJs = null; ctx.state.injectedFn = null; ctx.state.childCtx = null; ctx.refs.canvas = ctx.root.getElementById('dv-master-canvas'); ctx.refs.styleTag = ctx.root.getElementById('dv-master-styles'); }, update: (ctx) => { const { props, refs, state, frame } = ctx; if (!refs.canvas || !refs.styleTag) return; if (state.lastHtml !== props.htmlCode) { refs.canvas.innerHTML = props.htmlCode || ''; state.lastHtml = props.htmlCode; state.injectedFn = null; state.lastJs = null; } if (state.lastCss !== props.cssCode) { refs.styleTag.textContent = props.cssCode || ''; state.lastCss = props.cssCode; } const currentJs = (props.jsCode || '').trim(); if (currentJs && state.lastJs !== currentJs) { state.lastJs = currentJs; try { const sandbox = { renderDVGE: null }; const sandboxCode = `(function(sandbox, ctx) { \"use strict\"; const window = { renderDVGE: null }; ${currentJs} \\n sandbox.renderDVGE = window.renderDVGE; })(arguments[0], arguments[1])`; const fn = new Function(sandboxCode); fn(sandbox, ctx); if (sandbox.renderDVGE) { state.injectedFn = sandbox.renderDVGE; state.childCtx = { ...ctx, state: {}, refs: {}, parent: ctx }; } } catch (e) { console.error('[Studio Master] JS Error:', e.message); } } if (state.injectedFn) { try { state.childCtx.frame = frame; state.childCtx.props = props; state.childCtx.timeline = ctx.timeline; state.injectedFn(frame, props, state.childCtx); } catch (e) {} } } });"
      }
    },
    {
      "id": "template-overlay",
      "name": "Live Overlay Base",
      "description": "Plantilla base de mosca / overlay en esquina.",
      "version": "1.0.0",
      "author": "Jonatan Barón",
      "updatedAt": "2026-04-30T09:00:00.000Z",
      "files": {
        "manifest": {
          "id": "template-overlay",
          "name": "Live Overlay Base",
          "description": "Plantilla base de mosca.",
          "version": "1.0.0",
          "author": "Jonatan Barón",
          "presets": ["branding", "motion"],
          "schema": [
            { "id": "masterRules", "type": "prompt", "label": "Contexto para IA", "defaultValue": "Reglas" },
            { "id": "htmlCode", "type": "code", "label": "Estructura HTML", "defaultValue": "<div id=\"overlay-root\">\n  <div id=\"overlay-logo\">LIVE</div>\n</div>" },
            { "id": "cssCode", "type": "code", "label": "Estilos CSS", "defaultValue": "#overlay-root { position: absolute; top: 50px; right: 50px; background: red; padding: 10px 20px; border-radius: 8px; }\n#overlay-logo { color: white; font-size: 40px; font-weight: bold; }" },
            { "id": "jsCode", "type": "code", "label": "Lógica JS", "defaultValue": "window.renderDVGE = (frame, props, ctx) => {\n  const root = ctx.root.getElementById('overlay-root');\n  if (!root) return;\n  const ip = ctx.timeline.introProgress;\n  root.style.opacity = ip;\n  root.style.transform = `scale(${ctx.utils.lerp(0.8, 1, ctx.utils.easeOutBounce(ip))})`;\n};" }
          ]
        },
        "html": "<div id=\"dv-master-host\"><style id=\"dv-master-styles\"></style><div id=\"dv-master-canvas\" style=\"width: 1920px; height: 1080px; position: relative; overflow: hidden;\"></div></div>",
        "css": "#dv-master-host { width: 100%; height: 100%; background: transparent; will-change: transform; contain: paint; } #dv-master-canvas { transform-origin: top left; }",
        "js": "dvEngine.register({ awake: (ctx) => { ctx.state.lastHtml = null; ctx.state.lastCss = null; ctx.state.lastJs = null; ctx.state.injectedFn = null; ctx.state.childCtx = null; ctx.refs.canvas = ctx.root.getElementById('dv-master-canvas'); ctx.refs.styleTag = ctx.root.getElementById('dv-master-styles'); }, update: (ctx) => { const { props, refs, state, frame } = ctx; if (!refs.canvas || !refs.styleTag) return; if (state.lastHtml !== props.htmlCode) { refs.canvas.innerHTML = props.htmlCode || ''; state.lastHtml = props.htmlCode; state.injectedFn = null; state.lastJs = null; } if (state.lastCss !== props.cssCode) { refs.styleTag.textContent = props.cssCode || ''; state.lastCss = props.cssCode; } const currentJs = (props.jsCode || '').trim(); if (currentJs && state.lastJs !== currentJs) { state.lastJs = currentJs; try { const sandbox = { renderDVGE: null }; const sandboxCode = `(function(sandbox, ctx) { \"use strict\"; const window = { renderDVGE: null }; ${currentJs} \\n sandbox.renderDVGE = window.renderDVGE; })(arguments[0], arguments[1])`; const fn = new Function(sandboxCode); fn(sandbox, ctx); if (sandbox.renderDVGE) { state.injectedFn = sandbox.renderDVGE; state.childCtx = { ...ctx, state: {}, refs: {}, parent: ctx }; } } catch (e) { console.error('[Studio Master] JS Error:', e.message); } } if (state.injectedFn) { try { state.childCtx.frame = frame; state.childCtx.props = props; state.childCtx.timeline = ctx.timeline; state.injectedFn(frame, props, state.childCtx); } catch (e) {} } } });"
      }
    }
  ]
}